<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">
    <RadioGroup
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:orientation="horizontal">
        <RadioButton
            android:id="@+id/rbtn_live"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:button="@null"
            android:textColor="@drawable/selector_vedio_btn_tc"
            android:background="@drawable/selector_vedio_btn_bg"
            android:text="@string/zhibo"
            android:gravity="center"
            android:textSize="14sp"
            android:checked="false" />
        <RadioButton
            android:id="@+id/rbtn_vod"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:button="@null"
            android:textColor="@drawable/selector_vedio_btn_tc"
            android:background="@drawable/selector_vedio_btn_bg"
            android:text="@string/dianbo"
            android:gravity="center"
            android:textSize="14sp"
            />
    </RadioGroup>
<FrameLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/vedio_containerId"></FrameLayout>
</LinearLayout>